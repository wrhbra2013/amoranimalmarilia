<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transparência - Amor Animal</title>
    <link rel="stylesheet" href="/static/css/transparencia_dashboard.css">
    <link rel="stylesheet" href="/static/css/style.css">
    <link rel="stylesheet" href="/static/css/imagem/bootstrap-icons.min.css">
</head>
<body>
    <%- include('partials/header') %>

    <div class="transparencia-container">
        <h1>Portal da Transparência</h1>
        <p>Acesso aos documentos e relatórios da ONG Amor Animal.</p>

        <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
            <div class="admin-bar">
                <span>Área Administrativa: </span>
                <a href="/transparencia/form"><i class="bi bi-upload"></i> Novo Documento</a>
            </div>
        <% } %>

        <div class="dashboard-grid">
            <% 
            const icons = {
                'estatuto': 'bi-book',
                'diretoria': 'bi-people',
                'balanco': 'bi-bank',
                'financeiro': 'bi-file-earmark-text',
                'atividades': 'bi-list-check',
                'colaboracao': 'bi-briefcase',
                'plano': 'bi-clipboard-check'
            };
            %>
            <% Object.keys(types).forEach(key => { %>
                <a href="/transparencia/lista/<%= key %>" class="dashboard-card">
                    <i class="bi <%= icons[key] || 'bi-file-text' %>"></i>
                    <h3><%= types[key] %></h3>
                </a>
            <% }); %>
        </div>
        
        <div style="margin-top: 30px;">
            <a href="/home" class="btn-voltar"><i class="bi bi-arrow-left"></i> Voltar para Home</a>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>