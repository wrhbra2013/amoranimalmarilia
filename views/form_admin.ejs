  <%-include("_semmenu")-%>
  <div class="container mt-5">
      <!-- Sugestão: Usar btn-secondary para ações secundárias -->
      <a href="/" class="btn btn-secondary">Voltar para a página inicial</a>
  </div> 
  
  <div class="container mt-5">
      <h1>Criar Usuário</h1>
  
      <!-- NOVO: Bloco para exibir mensagens de feedback -->
      <% if (error && error.length > 0) { %>
          <div class="alert alert-danger" role="alert">
              <%= error %>
          </div>
      <% } %>
      <% if (success && success.length > 0) { %>
          <div class="alert alert-success" role="alert">
              <%= success %>
          </div>
      <% } %>
  
      <form action="/admin/form" method="post"> 
          <div class="mb-3"> 
              <label for="usuario" class="form-label">Usuário</label>
              <input type="text" class="form-control" id="usuario" name="usuario" placeholder="Novo usuário" required>
          </div>
          <div class="mb-3">
              <label for="senha" class="form-label">Senha</label>
              <!-- CORREÇÃO: Alterado para type="password" por segurança -->
              <input type="password" class="form-control" id="senha" name="senha" placeholder="Nova senha" required>
          </div>
          <div class="mb-3">
             <!-- CORREÇÃO: O 'for' agora aponta para 'confirmar' -->
             <label for="confirmar" class="form-label">Confirmar Senha</label>
             <!-- CORREÇÃO: Alterado para type="password" por segurança -->
             <input type="password" class="form-control" id="confirmar" name="confirmar" placeholder="Confirmar senha" required>
         </div>
          <!-- Checkbox para mostrar/esconder senha -->
          <div class="mb-3 form-check">
              <input type="checkbox" class="form-check-input" id="mostrarSenha">
              <label class="form-check-label" for="mostrarSenha">Mostrar Senha</label>
          </div>
         
          <button type="submit" class="btn btn-primary">Criar Usuário</button>
      </form>
  </div>
  
<%-include("_footer")-%>

<script src="/static/js/form_admin.js"></script>
 