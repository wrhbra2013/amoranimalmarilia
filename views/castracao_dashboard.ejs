<link rel="stylesheet" href="/static/css/castracao_dashboard.css">
<%- include("_header")-%>
<div class="dashboard-container">
    <header class="dashboard-header">
        <h1>Central de Castração</h1>
        <p>Escolha o tipo de serviço de castração que melhor atende às suas necessidades. Todos os nossos serviços são realizados com profissionais qualificados e acessíveis.</p>

        <% if (user && user.isAdmin) { %>
        <form action="/castracao/admin/default-clinica" method="POST" class="mt-3 admin-default-clinica">
            <label class="form-label fw-bold">Clínica padrão para Baixo Custo e Pets de Rua (modo Administrador)</label>
            <div style="display:flex; gap:8px; align-items:center; max-width:720px;">
                <select id="select-default-clinica" name="defaultClinica" class="form-select" style="max-width:400px;">
                    <option value="">-- Nenhuma (A definir) --</option>
                    <% if (clinicas && clinicas.length) { %>
                        <% clinicas.forEach(function(c) { %>
                            <option value="<%= c.nome %>" <%= (defaultClinica === c.nome) ? 'selected' : '' %> data-endereco="<%= c.endereco ? c.endereco.replace(/"/g,'&quot;') : '' %>"><%= c.nome %></option>
                        <% }); %>
                    <% } %>
                </select>
                <button type="submit" class="btn btn-primary">Salvar clínica padrão</button>
            </div>
            <div style="margin-top:6px; max-width:720px;">
                <small class="text-muted">Endereço da clínica padrão:</small>
                <div id="default-clinica-endereco" style="font-weight:500;"> <%= (typeof defaultClinicaEndereco !== 'undefined' && defaultClinicaEndereco) ? defaultClinicaEndereco : (defaultClinica ? 'Endereço não cadastrado' : '— A definir —') %> </div>
            </div>
        </form>
        <% } %>
    </header>

    <div class="dashboard-buttons">
        <!-- Castração de Baixo Custo -->
        <div class="dashboard-card dashboard-card-highlighted">
            <div class="card-content">
                <div class="card-icon">
                    <i class="bi bi-currency-dollar"></i>
                </div>
                <h2 class="card-title">Castração de Baixo Custo</h2>
                <p class="card-description">
                    Serviço de castração contínua com valores subsidiados para tutores que desejam realizar o procedimento de forma econômica e segura.
                </p>
                <a href="/castracao/baixo-custo" class="btn-dashboard btn-primary">
                    <i class="bi bi-plus-circle me-2"></i>Agendar Castração
                </a>
            </div>
        </div>

        <!-- Mutirão de Castração -->
        <div class="dashboard-card">
            <div class="card-content">
                <div class="card-icon">
                    <i class="bi bi-people-fill"></i>
                </div>
                <h2 class="card-title">Calendário de Mutirão</h2>
                <p class="card-description">
                    Eventos especiais com condições diferenciadas para castração em grupo. Acompanhe nosso calendário para participar do próximo mutirão.
                </p>
                <a href="/castracao/calendario-mutirao" class="btn-dashboard btn-secondary">
                    <i class="bi bi-calendar-event me-2"></i>Ver Calendário de Mutirões
                </a>
            </div>
        </div>

        <!-- Pets de Rua -->
        <div class="dashboard-card">
            <div class="card-content">
                <div class="card-icon">
                    <i class="bi bi-heart-fill"></i>
                </div>
                <h2 class="card-title">Pets de Rua</h2>
                <p class="card-description">
                    Programa especial para castração de animais de rua. Trabalhamos com protetores e voluntários para controlar a população de animais abandonados.
                </p>
                <a href="/castracao/pets-rua" class="btn-dashboard btn-info">
                    <i class="bi bi-house-heart me-2"></i>Cadastrar Pet de Rua
                </a>
            </div>
        </div>
    </div>

    

        

<%- include("_footer")-%>

<script>
    (function(){
        var select = document.getElementById('select-default-clinica');
        var enderecoEl = document.getElementById('default-clinica-endereco');

        function updateEndereco() {
            var opt = select.options[select.selectedIndex];
            var endereco = opt && opt.dataset ? opt.dataset.endereco : (opt && opt.getAttribute ? opt.getAttribute('data-endereco') : '');
            if (!endereco) endereco = (select.value ? 'Endereço não cadastrado' : '— A definir —');
            enderecoEl.textContent = endereco;
        }

        if (select && enderecoEl) {
            select.addEventListener('change', updateEndereco);
            // inicializa
            updateEndereco();
        }
    })();
</script>