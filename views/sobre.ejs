<%- include("_header")-%>

<main class="main-content">
    <section id="sobre-page" class="section-sobre-page">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <div class="sobre-content">
                        <h2>Sobre Nós</h2>
                        <p>A ONG Amor Animal é uma organização dedicada ao resgate, cuidado e proteção de animais abandonados. Trabalhamos incansavelmente para garantir que cada animal tenha uma segunda chance em um lar amoroso.</p>
                        <p>Nossa equipe é formada por voluntários apaixonados por animais, que dedicam seu tempo e esforço para fazer a diferença na vida de cada um deles.</p>
                        <p>Além do resgate e adoção, também promovemos a conscientização sobre a importância da posse responsável e do respeito aos animais.</p>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="sobre-image">
                        <img src="/static/css/imagem/ong2.jpg" alt="Imagem da ONG Amor Animal" class="img-fluid">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="voluntarios" class="py-5 bg-light">
        <div class="container">
            <h2 class="text-center mb-4">Familia Amor Animal Marilia</h2>
            <div class="voluntarios-carousel-container">
                <div class="voluntarios-scroll-container">
                    <% if (model && model.length > 0) { %>
                        <% model.forEach(voluntario => { %>
                            <div class="card voluntario-card">
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title"><i class="fas fa-user-circle me-2"></i><%= voluntario.nome %></h5>
                                    <h6 class="card-subtitle mb-2 text-muted"><i class="fas fa-map-marker-alt me-2"></i><%= voluntario.localidade %></h6>
                                    <p class="card-text"><strong>Habilidade:</strong> <%= voluntario.habilidade %></p>
                                    <blockquote class="blockquote mt-auto mb-0">
                                        <p class="mb-0 fst-italic">"<%= voluntario.mensagem %>"</p>
                                    </blockquote>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="text-center w-100">
                            <p class="text-muted">Nenhum voluntário cadastrado no momento.</p>
                        </div>
                    <% } %>
                </div>

                <% if (model && model.length > 3) { %>
                    <button class="voluntarios-carousel-control prev" aria-label="Anterior">&#10094;</button>
                    <button class="voluntarios-carousel-control next" aria-label="Próximo">&#10095;</button>
                <% } %>
            </div>
        </div>
    </section>

    <div class="container mt-4 mb-5">
        <div class="text-center">
            <h1>Quer fazer parte da causa PET?</h1>
            <p class="lead">Saiba como a sua CONTRIBUIÇÃO pode ajudar!</p>
        </div>
        <div class="text-center">
            <h2>Procure nossos canais.</h2>
         
        </div>
    </div>


    
</main>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const carouselContainer = document.querySelector('.voluntarios-carousel-container');
    if (!carouselContainer) return;

    const container = carouselContainer.querySelector('.voluntarios-scroll-container');
    const prevBtn = carouselContainer.querySelector('.voluntarios-carousel-control.prev');
    const nextBtn = carouselContainer.querySelector('.voluntarios-carousel-control.next');

    if (container && prevBtn && nextBtn) {
        const card = container.querySelector('.voluntario-card');
        if (!card) return;

        // Calcula a quantidade de rolagem: a largura do card + o gap.
        const containerStyle = window.getComputedStyle(container);
        const cardWidth = card.offsetWidth;
        const gap = parseFloat(containerStyle.gap) || 16; // 1rem = 16px como fallback
        const scrollAmount = cardWidth + gap;

        prevBtn.addEventListener('click', () => {
            container.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
        });

        nextBtn.addEventListener('click', () => {
            container.scrollBy({ left: scrollAmount, behavior: 'smooth' });
        });
    }
});
</script>

    <%- include("_footer")-%>