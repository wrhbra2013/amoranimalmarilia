<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Identificação - Transparência</title>
    <link rel="stylesheet" href="/static/css/style.css">
    <link rel="stylesheet" href="/static/css/imagem/bootstrap-icons.min.css">
    <style>
        .auth-container {
            max-width: 500px;
            margin: 50px auto;
            padding: 30px;
            background: var(--container-bg);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            box-shadow: 0 4px 15px var(--container-shadow);
        }
        .auth-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .auth-header i {
            font-size: 3rem;
            color: var(--brand-teal);
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-control {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        .btn-submit {
            width: 100%;
            padding: 12px;
            background-color: var(--brand-teal);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background 0.3s;
        }
        .btn-submit:hover {
            background-color: var(--brand-teal-dark);
        }
        .alert {
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        .alert-info {
            background-color: #d1ecf1;
            color: #0c5460;
            border-color: #bee5eb;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <div class="auth-container">
        <div class="auth-header">
            <i class="bi bi-shield-lock"></i>
            <h2>Dados Sensiveis.</h2>
            <p>Portal da Transparência</p>
            <fieldset>
                Por se tratar de informações fiscais e financeiras, por gentileza, solicitamos que se identifique para prosseguir, tornado o acesso seguro e controlado.
            </fieldset>
        </div>

        <% if (message) { %>
            <div class="alert <%= message.includes('sucesso') ? 'alert-info' : 'alert' %>">
                <%= message %>
            </div>
        <% } %>

        <% if (step === 'login') { %>
            <form action="/transparencia/identificacao" method="POST">
                <div class="form-group">
                    <label for="cpf">Digite seu CPF para acessar:</label>
                    <input type="text" id="cpf" name="cpf" class="form-control" placeholder="000.000.000-00" required value="<%= cpfValue || '' %>">
                </div>
                <button type="submit" class="btn-submit">Entrar</button>
            </form>
        <% } else { %>
            <form action="/transparencia/cadastro" method="POST">
                <input type="hidden" name="cpf" value="<%= cpfValue %>">
                <div class="form-group"><label>CPF:</label><input type="text" class="form-control" value="<%= cpfValue %>" disabled></div>
                <div class="form-group"><label>Nome Completo:</label><input type="text" name="nome" class="form-control" required></div>
                <div class="form-group"><label>Organização (Opcional):</label><input type="text" name="organizacao" class="form-control"></div>
                <div class="form-group"><label>Email:</label><input type="email" name="email" class="form-control" required></div>
                <div class="form-group"><label>Telefone:</label><input type="text" name="telefone" class="form-control" required></div>
                <button type="submit" class="btn-submit">Solicitar Acesso</button>
            </form>
        <% } %>
    </div>

    <%- include('partials/footer') %>
</body>
</html>